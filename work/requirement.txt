Django==5.0.6
django-crispy-forms==1.14.0
gunicorn==20.1.0
psycopg2==2.9.6  # For PostgreSQL database support (if you are using PostgreSQL)
django-environ==0.8.1  # For handling environment variables
whitenoise==6.0.0  # For serving static files in production

# Additional dependencies for handling HTML, CSS, and templates (if needed):
django-staticfiles==1.0.0  # For static files handling (if not using Whitenoise)
